const superHero: string[] = []; 
const heroPower: number[] = []; 
const heroPower1: Array<number> = []; //same as above in other way

type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [];

const MLModels: number[][] = [
    [255, 255, 255],
    [123, 123, 123]
]


superHero.push("spiderman");
heroPower.push(2)
heroPower1.push(2)
allUsers.push({name: 'Mohit', isActive: true});

export {}